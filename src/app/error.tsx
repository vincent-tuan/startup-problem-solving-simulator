"use client";
export default function GlobalError({ reset }: { error: Error & { digest?: string }; reset: () => void }) { return <main className="container-page grid min-h-screen place-items-center"><div className="surface max-w-lg p-8 text-center"><div className="eyebrow">Recovery state</div><h1 className="mt-3 text-3xl font-black">The interface hit an unexpected state.</h1><p className="muted mt-3">Your last accepted command remains saved. Reload the canonical cloud state to continue.</p><button className="btn btn-primary mt-7" onClick={reset}>Reload state</button></div></main>; }
